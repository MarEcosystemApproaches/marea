% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ea_data_class.R
\name{ea.subset}
\alias{ea.subset}
\title{Filter an \code{ea_data} Object by Column and Value}
\usage{
ea.subset(x, column, value)
}
\arguments{
\item{x}{An \code{ea_data} object.}

\item{column}{A character string naming the column in the \code{data} slot to filter by.}

\item{value}{A value or vector of values to match in the specified column.}
}
\value{
A new \code{ea_data} object containing only the matching rows.
}
\description{
A convenient helper function to filter the \code{data} slot of an \code{ea_data} object
based on matching values in a specified column.
}
\examples{
df <- data.frame(year = rep(2000:2002, 2),
                 temp_c = rnorm(6),
                 group = rep(c("A", "B"), each = 3))
obj <- ea_data(df, value_col = c("temp_c"), data_type = "temperature",
               region = "Test Region", location_descriptor = "surface",
               units = "°C")

# Filter for a single year
ea.subset(obj, "year", 2001)

# Filter for specific groups
ea.subset(obj, "group", "B")

}
